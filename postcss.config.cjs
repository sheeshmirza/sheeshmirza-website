module.exports = {
  // PostCSS plugins extend CSS capabilities and ensure cross-browser support
  plugins: {
    // Automatically adds vendor prefixes for better browser compatibility
    autoprefixer: {},
    // Enables Tailwind CSS utility generation and processing
    tailwindcss: {},
  },
};